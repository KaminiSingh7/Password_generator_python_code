#PASSWORD GENERATOR USING PYTHON:-

import random 
import string

pass_len = int(input("Enter the length of your password which you want to be generate:"))
charval = string.digits + string.ascii_letters + string.punctuation
password = ""
for i in range(pass_len):
    password += random.choice(charval)

print("Your high security password is:", password)
